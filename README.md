# SumiShisen

## Description
A simple solver for Minigames in **Sumikko Farm**

## How to Use
* For the sumi, tap the `A`~`O` first, then tap the corresponding position above
* For the same sumi, make sure to mark as the same alphabet
* For the box, tap the `Box` first, then tap the corresponding position above
* To delete the filled position, tap the `Del` first, the tap the corresponding postion above
* To clear all the filled position, long tap the `Del`
* After all of the fill in, tap the `OK` to start
* In the result view, tap `Pre` for previous step, tap `Next` for next step, tap `Close` to exit the result
